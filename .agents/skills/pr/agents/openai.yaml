interface:
  display_name: "PR"
  short_description: "Create/update a PR, post a structured Opus review, and record it in my-calendar."
  default_prompt: "Create a lightweight PR, generate a JSON-Schema-validated Claude Opus 5 review through Teamorouter, post the verified GitHub comment, and record it in my-calendar."
